{
  "schema_version": 1,
  "updated_at": "2026-08-04T23:13:10+08:00",
  "project": "biohub-cell-tracking",
  "active_milestone": "Concrete learned training and inference pipeline",
  "status": "complete",
  "host": "windows",
  "completed": [
    "Preserved the validated classical public-test baseline as a fallback",
    "Implemented same-stem training Zarr/GEFF discovery and validated GEFF graph loading",
    "Implemented lazy positive-centered 3D training patches with physical Gaussian targets",
    "Implemented an encoder-decoder 3D U-Net, BCE-plus-Dice fitting, dataset-level validation splits, best-checkpoint retention, and TorchScript/manifest export",
    "Implemented multi-seed detector ensemble training and overlapping sliding-window inference with flip TTA",
    "Implemented sparse continuation, gap, and atomic division candidates with geometry, motion, confidence, appearance, intensity, volume, and temporal-context features",
    "Implemented class-balanced learned association fitting, portable JSON artifacts, and runtime learned scoring",
    "Implemented ILP lineage optimization with single-parent and single-event constraints plus greedy fallback",
    "Added train-detector, train-detector-ensemble, train-association, and learned run CLI paths",
    "Documented training and inference commands and kept artifact outputs gitignored",
    "Verified 58 tests, Ruff, strict mypy, learned-branch inference, artifact round trips, CLI discovery, and config parsing"
  ],
  "in_progress": [],
  "blocked": [],
  "last_verified_commands": [
    ".venv\\Scripts\\python.exe -m pytest -q --basetemp outputs\\pytest_tmp (58 passed)",
    ".venv\\Scripts\\python.exe -m ruff check . (passed)",
    ".venv\\Scripts\\python.exe -m mypy src (42 source files, no issues)",
    ".venv\\Scripts\\python.exe -m biohub_tracker.cli --help (training and inference commands listed)",
    ".venv\\Scripts\\python.exe -c \"from biohub_tracker.config import load_config; from biohub_tracker.training.config import load_training_config; c=load_config('configs/architecture.yaml'); t=load_training_config('configs/training.yaml'); print(c.detection_method, c.association_scorer_kind, t.detector_seeds)\" (learned learned_linear (42, 137))"
  ],
  "next_action": "On a host with the official training stores and PyTorch/GPU, run train-detector-ensemble and train-association, then validate the learned submission against the preserved baseline",
  "needs_human": [
    "This Windows checkout currently has no data/competition/train directory and no PyTorch installation, so full fitting was not executed here"
  ],
  "artifacts": {
    "architecture": "docs/ARCHITECTURE.md",
    "training_config": "configs/training.yaml",
    "learned_config": "configs/architecture.yaml",
    "validated_baseline_submission": "outputs/public_test_baseline/submission/submission.csv",
    "baseline_report": "outputs/public_test_baseline/BASELINE_REPORT.md"
  }
}
